const monk = require('monk');
const dbuser = 'user'
const dbassword = 'password'
const dbame = 'name'
const dbervername = 'servername'

const connectionString = `mongodb+srv://${dbuser}:${dbpassword}@${dbservername}/${dbname}`;

const db = monk(connectionString);

module.exports = db;